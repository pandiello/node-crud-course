<% if (success.length > 0) { %>
    <div class="alert alert-success"><%= success %></div>
<% } %>

<div class="jumbotron text-center">
    <h1><%= place.name %></h1>
    <p><%= place.description %></p>
</div>
<h3>Partes</h3>
<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Fecha</th>
        </tr>
    </thead>
    <tbody>

        <% for (var registry of  place.registries) { %>
            <tr>
                <td><%= registry.name %></td>
                <td><%= registry.description %></td>
                <td><%= dateformat(registry.date,"yyyy-mm-dd") %></td>
            </tr>
        <% } %>

    </tbody>
</table>
